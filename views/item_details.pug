extends layout

block content
  div
    div
      h3 #{item_details.name}
      a(href=`/category/authentication/${item_details._id}`) Update
        img(src='/')
      a(href=`/category/deleteItem/${item_details._id}`) Delete
        img(src='/')
    img(src=item_details.image.imageUrl)
    h4 By: #{item_details.author}
    h4 Genre: #{item_details.genre}
    p #{item_details.details}
  div
    p #{item_details.category}
