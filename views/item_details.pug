extends layout

block content
  div
    h3 #{item_details.name}
    - console.log('Name:', item_details.name)
    img(src='/')
    h4 By: #{item_details.author}
    h4 Genre: #{item_details.genre}
    p #{item_details.details}
  div
    p #{item_details.category}
